server:
  port: 8087

Spring:
  application:
    name: API-GATEWAY

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    eureka-service-urls:
      defaultZone: http://localhost:8761/eureka/